# File: src/db.properties
# Placed at the root of the src folder so Eclipse copies it straight into
# WEB-INF/classes on build, putting it on the runtime classpath.
db.driver=org.postgresql.Driver
db.url=jdbc:postgresql://localhost:5432/medical_db
db.username=postgres
db.password=3693